name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  golang:
    uses: ./.github/workflows/golang.yaml
    with:
      golang_version: "1.24.0"

  helm:
    uses: ./.github/workflows/helm.yaml
